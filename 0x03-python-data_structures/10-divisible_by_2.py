#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    is_it_div = []

    for d in range(len(my_list)):
        if my_list[i] % 2 == 0:
            is_it_div.append(True)
        else:
            is_it_div.append(False)

    return (is_it_div)
